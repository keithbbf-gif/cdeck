{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "cDeck main window — IPC only; COSMOS HTTP is proxied in Rust",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
